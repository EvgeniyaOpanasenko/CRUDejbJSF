package facades.interfaces;

//The DogFacadeImp class does all the work of protecting the DogDAO from the view.
// It might have the business rules and avoid access to the database
// if some data is missing or any other business rule has being broken.
public abstract class AbractFacade<T> {
}
